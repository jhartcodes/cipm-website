---
/**
 * IntroductionSection Component
 *
 * Sits directly below the hero. Displays a headline and optional
 * supporting text. Variant C style: turquoise background, white text,
 * compact spacing.
 */

import Container from "../../layout/Container.astro";
import Text from "../../ui/Text.astro";

interface Props {
  headline?: string;
  supportingText?: string;
}

const { headline, supportingText } = Astro.props;

if (!headline) return;
---

<section class="relative overflow-visible bg-turquoise py-12 lg:py-16">
  <div
    class="pointer-events-none absolute inset-x-0 -top-28 h-48 bg-gradient-to-t from-turquoise/100 via-turquoise-light/25 to-transparent blur-2xl"
    aria-hidden="true"
  ></div>

  <Container size="wide" class="relative z-10">
    <div>
      <Text as="h2" variant="headingLg" tone="inverted">
        {headline}
      </Text>

      {
        supportingText && (
          <p class="mt-3 text-base text-white/90 max-w-xl">{supportingText}</p>
        )
      }
    </div>
  </Container>
</section>
